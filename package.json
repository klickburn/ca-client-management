{
  "name": "ca-client-management",
  "version": "2.0.0",
  "description": "A web-based client database management application for CA firms in India",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend-v2 && npm run dev",
"install-all": "npm install && cd backend && npm install && cd ../frontend-v2 && npm install",
    "setup": "node backend/seed.js",
    "migrate-roles": "node backend/migrate-roles.js",
    "build": "cd frontend-v2 && npm run build",
    "render-build": "npm install && npm run install-all && npm run build"
  },
  "keywords": [
    "client-management",
    "ca",
    "accounting",
    "mongodb",
    "express",
    "react",
    "node"
  ],
  "author": "KlickBurn",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
